program sample11pp;
    procedure kazuyomikomi(n : integer);
    begin
        writeln('input the number of data');
        readln(n)
    end;
    procedure kazuyomikomi(x : integer);
        begin
            writeln('input the number of data');
            readln(x)
        end;

    var x: integer;
    begin
        call kazuyomikomi(x)
    end.