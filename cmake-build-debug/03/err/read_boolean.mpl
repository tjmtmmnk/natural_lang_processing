program sample11pp;
    procedure kazuyomikomi(n : boolean);
    begin
        writeln('input the number of data');
        readln(n)
    end;
    var
        n: boolean;
    begin
        call kazuyomikomi(n)
    end.